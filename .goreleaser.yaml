version: 2

builds:
  - main: ./cmd/cwlogs
    binary: cwlogs
    ldflags:
      - -s -w -X main.version={{.Version}}
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

brews:
  - repository:
      owner: capybara-translation
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/capybara-translation/cwlogs"
    description: "Print AWS CloudWatch Logs events"
    license: "MIT"
    install: |
      bin.install "cwlogs"
    test: |
      system "#{bin}/cwlogs", "--version"

changelog:
  sort: asc
